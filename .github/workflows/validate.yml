name: validate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  catalogue:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check the catalogue
        run: node .github/validate.mjs
